function ShowElementeAndTransition() {
    var Tag = window.document.querySelector('.sobre');
    Tag.style.opacity = '1';  // Torna o elemento visível
    Tag.style.right = '50px';  // Move para a posição original
}
